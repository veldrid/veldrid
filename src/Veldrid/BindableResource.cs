namespace Veldrid
{
    /// <summary>
    /// A resource which can be bound in a <see cref="ResourceSet"/> and used in a shader.
    /// See <see cref="UniformBuffer"/>, <see cref="TextureView"/> and <see cref="Sampler"/>.
    /// </summary>
    public interface BindableResource { }
}
