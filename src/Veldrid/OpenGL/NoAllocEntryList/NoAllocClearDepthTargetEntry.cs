namespace Veldrid.OpenGL.NoAllocEntryList
{
    internal struct NoAllocClearDepthTargetEntry
    {
        public readonly float Depth;

        public NoAllocClearDepthTargetEntry(float depth)
        {
            Depth = depth;
        }
    }
}